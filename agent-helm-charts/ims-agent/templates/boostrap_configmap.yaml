{{- if .Values.bootstrapJob.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-bootstrap-config
data:
  ims_agent_bootstrap_config.yaml: |-
{{ .Files.Get "config/ims_agent_bootstrap_config.yaml" | indent 4 }}
  ims_agent.yaml: |-
{{ .Files.Get "config/ims_agent.yaml" | indent 4 }}
{{- end }}