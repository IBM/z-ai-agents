{{- if .Values.bootstrapJob.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-bootstrap-config
data:
  db2_agent_bootstrap_config.yaml: |-
{{ .Files.Get "config/db2_agent_bootstrap_config.yaml" | indent 4 }}
  db2_agent.yaml: |-
{{ .Files.Get "config/db2_agent.yaml" | indent 4 }}
{{- end }}