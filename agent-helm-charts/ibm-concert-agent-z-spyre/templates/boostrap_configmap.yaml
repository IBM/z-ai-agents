{{- if .Values.bootstrapJob.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-bootstrap-config
data:
  ibm_concert_agent_spyre_bootstrap_config.yaml: |-
{{ .Files.Get "config/ibm_concert_agent_spyre_bootstrap_config.yaml" | indent 4 }}
  ibm_concert_agent_granite.yaml: |-
{{ .Files.Get "config/ibm_concert_agent_granite.yaml" | indent 4 }}
{{- end }}