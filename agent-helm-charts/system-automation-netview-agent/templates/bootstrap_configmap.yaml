{{- if .Values.bootstrapJob.enabled }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Chart.Name }}-bootstrap-config
data:
  system_automation_netview_agent_bootstrap_config.yaml: |-
{{ .Files.Get "config/system_automation_netview_agent_bootstrap_config.yaml" | indent 4 }}
  system_automation_netview_agent.yaml: |-
{{ .Files.Get "config/system_automation_netview_agent.yaml" | indent 4 }}
{{- end }}